import type { FC } from 'react';

const AppLayout: FC = ({ children }) => {
  return (
    <>
      {/* nav */}
      {/* body */}
      {/* children */}
      {/* footer */}
    </>
  );
};

export default AppLayout;
