import styled from 'styled-components';
import { useIsMobile } from '@src/hook/useIsMobile';

const About = () => {
  return <AboutSectionWrapper></AboutSectionWrapper>;
};

export default About;

const AboutSectionWrapper = styled.section``;
